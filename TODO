use -random to set rotation, then set lat/lon other ways

use arbitrary "lookAt" position

add spice objects to planet list, not annotation map

multiple line markers

Uranus rings

constant latitude arcs
